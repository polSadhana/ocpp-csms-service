# Default profile (dev for development, prod for production)
spring.profiles.active=dev

# Common JPA settings
# Disable open-in-view to prevent the 'Open Session in View' anti-pattern
# This helps identify N+1 query issues during development and improves performance
spring.jpa.open-in-view=false
