# Default profile (dev for development, prod for production)
spring.profiles.active=dev

# Common JPA settings
# Disable open-in-view to avoid potential performance issues with lazy loading in web contexts
spring.jpa.open-in-view=false
